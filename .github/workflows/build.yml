name: Build - Container Build & Push

on:
  workflow_call:
    inputs:
      image_tag:
        required: true
        type: string

permissions:
  contents: read

jobs:

  # ============================================================
  # GATE 5: BUILD - Build & Save Docker Images as Artifacts
  # ============================================================
  build:
    name: Build - ${{ matrix.service }}
    runs-on: ubuntu-latest
    strategy:
      matrix:
        service: [backend, frontend, ollama]
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Set Up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build Docker Image
        run: |
          docker build -t nexabank-${{ matrix.service }}:${{ inputs.image_tag }} ./${{ matrix.service }}
          docker save nexabank-${{ matrix.service }}:${{ inputs.image_tag }} \
            -o nexabank-${{ matrix.service }}.tar

      - name: Upload Image Artifact
        uses: actions/upload-artifact@v4
        with:
          name: docker-image-${{ matrix.service }}
          path: nexabank-${{ matrix.service }}.tar

  # ============================================================
  # GATE 6: CONTAINER IMAGE SCAN - Trivy
  # Loads saved image artifact — no rebuild needed
  # ============================================================
  image_scan:
    name: Container Scan - ${{ matrix.service }}
    runs-on: ubuntu-latest
    needs: build
    strategy:
      matrix:
        service: [backend, frontend, ollama]
    steps:
      - name: Download Image Artifact
        uses: actions/download-artifact@v4
        with:
          name: docker-image-${{ matrix.service }}

      - name: Load Docker Image
        run: docker load -i nexabank-${{ matrix.service }}.tar

      - name: Cache Trivy DB
        uses: actions/cache@v4
        with:
          path: ~/.cache/trivy
          key: trivy-db-${{ runner.os }}-${{ github.run_id }}
          restore-keys: |
            trivy-db-${{ runner.os }}-

      - name: Run Trivy Image Scan
        uses: aquasecurity/trivy-action@master
        with:
          image-ref: nexabank-${{ matrix.service }}:${{ inputs.image_tag }}
          format: 'table'
          exit-code: '1'
          ignore-unfixed: true
          vuln-type: 'os,library'
          severity: 'CRITICAL,HIGH'
          output: trivy-${{ matrix.service }}-report.txt

      - name: Upload Trivy Report
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: trivy-scan-${{ matrix.service }}
          path: trivy-${{ matrix.service }}-report.txt

  # ============================================================
  # GATE 7: PUSH TO DOCKER HUB
  # Loads scanned image artifact — no rebuild needed
  # ============================================================
  push_to_hub:
    name: Push to Docker Hub - ${{ matrix.service }}
    runs-on: ubuntu-latest
    needs: image_scan
    strategy:
      matrix:
        service: [backend, frontend, ollama]
    steps:
      - name: Download Image Artifact
        uses: actions/download-artifact@v4
        with:
          name: docker-image-${{ matrix.service }}

      - name: Load Docker Image
        run: docker load -i nexabank-${{ matrix.service }}.tar

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Tag and Push Image
        run: |
          docker tag nexabank-${{ matrix.service }}:${{ inputs.image_tag }} \
            ${{ secrets.DOCKER_USERNAME }}/nexabank-${{ matrix.service }}:${{ inputs.image_tag }}
          docker push \
            ${{ secrets.DOCKER_USERNAME }}/nexabank-${{ matrix.service }}:${{ inputs.image_tag }}
